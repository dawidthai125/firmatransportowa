# TransFlow — osobny projekt Supabase (NIE wgdom)
# https://supabase.com/dashboard → New project → np. "transflow-prod"

project_id = "YOUR_PROJECT_REF"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[functions.transflow-api]
verify_jwt = false

[auth]
enabled = true
site_url = "http://127.0.0.1:5174"
additional_redirect_urls = ["https://*.vercel.app"]
