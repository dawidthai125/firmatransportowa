# Skopiuj do .env (lokalny dev). NIE używaj projektu wgdom — osobny Supabase!

# 1. https://supabase.com/dashboard → New project (np. transflow-prod)
# 2. Project Settings → General → Project ID
VITE_SUPABASE_PROJECT_ID=your-new-project-ref

# 3. Project Settings → API → Publishable key (sb_publishable_...)
VITE_SUPABASE_ANON_KEY=your-publishable-key

# Edge Function (domyślnie transflow-api)
VITE_SUPABASE_FUNCTION_SLUG=transflow-api

# --- Tryb wdrożenia (domyślnie: jedna firma, bez UI SaaS) ---
# company = strona firmy (Tajski-Trans) | saas = portal multi-tenant (przyszłość)
# VITE_DEPLOYMENT_MODE=company
# VITE_COMPANY_NAME=Tajski-Trans Sp. z o.o.
# VITE_COMPANY_SHORT_NAME=Tajski-Trans
# VITE_COMPANY_SLUG=TAAJSKI-TRANS

# Okres testów — wejście do paneli bez hasła (domyślnie włączone w trybie company)
# VITE_OPEN_TEST_ACCESS=true

# --- Auth (v0.10) ---
# Po migracji SQL utwórz użytkowników w Supabase Auth (email/hasło) lub
# zaloguj się emailem — pierwszy login rejestruje konto i wiąże z tenant_members.
# Wyłącz "Confirm email" w Auth settings dla szybszego demo.

# --- Tylko Supabase Dashboard / GitHub Secrets (NIE w Vercel frontend) ---
# SUPABASE_ACCESS_TOKEN=   → GitHub Actions deploy Edge Functions
# SUPABASE_PROJECT_REF=      → ten sam co VITE_SUPABASE_PROJECT_ID
